#-*-coding:utf8;-*-

name = "guangrei"

if name == "guangrei" then
    assert name == "guangrei"
elif name == "budi" then
    assert name == "budi"
else
    assert name != "guangrei" or name != "budi"
end
